id: P1-I05
title: Implement backend job list query
type: impl
status: done
goal: Expose a backend query path that returns the minimal job list and status view.
owned_scope:
- src-tauri/src/application/job/list/
- src-tauri/src/domain/job/list/
out_of_scope:
- job creation mutation path
- provider progress detail
depends_on:
- P1-C02
- P1-V02
produces:
- backend job list query path
consumes:
- job state model
- import-to-job acceptance anchor
acceptance_anchor:
- job list query returns the minimal observable state required by Phase 1
parallel_safe_with:
- P1-I04
- P1-I06
- P1-I07
shared_risks:
- allowing job detail scope to leak in would expand the query beyond the first list
  path
done_when:
- the backend can expose the first job list view without redesigning the state contract
