# frontend/src/ui

画面の表示と画面状態を置く。

- `components/` は複数画面で共有する表示部品を持つ。
- `screens/` は画面ごとの container、screen、表示変換、story、fixture、画面仕様を持つ。
- `stores/` は複数画面で共有する UI state を持つ。
- `styles/` は application 全体の style を持つ。
