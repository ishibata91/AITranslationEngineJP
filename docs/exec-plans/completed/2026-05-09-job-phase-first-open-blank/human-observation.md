# Human Observation: term translation phase first open missing

## 観測者

- 人間

## 観測日時

- 2026-05-09

## 対象

- `jobID1`
- 翻訳ジョブ一覧からジョブ段階へ進む画面遷移

## 操作

1. 初回起動時に `jobID1` を対象にする。
2. `ジョブ段階へ進む` を押す。

## 実際の結果

- 初回操作では、遷移後に少なくとも単語翻訳フェーズ UI が表示されない。
- 注意: 「何も表示されない」という表現は強すぎるため、観測記録では採用しない。
- 添付画像では、`ジョブの進み方` は表示されている。
- 添付画像では、`単語翻訳` が `現在の作業` として強調されている。
- 添付画像では、下部 panel が `ジョブ未選択` と表示している。
- 添付画像では、下部 panel が `未完了ジョブ一覧でジョブを選んでください` と表示している。
- 一度一覧へ戻る。
- 同じ `jobID1` で同じ操作を再実行すると、単語翻訳フェーズ UI が表示される。

## 期待との差分

- 期待: 初回操作でも単語翻訳フェーズ UI が表示される。
- 差分: 初回遷移では、進行カードは単語翻訳を現在作業として示すが、下部 panel はジョブ未選択として扱う。
- 差分: 初回遷移では、選択済み job が単語翻訳フェーズ UI へ渡っていない。

## 未確認事項

- browser console のエラーは未確認。
- backend log のエラーは未確認。
- 初回遷移時に frontend state が job detail を受け取っているかは未確認。
- 初回遷移時に routing だけ成功し、単語翻訳フェーズ data の取得が遅延しているかは未確認。
- 初回遷移時に job run target が途中で null へ戻っているかは未確認。
- `jobID1` 固有の永続データ問題か、任意 job で発生する問題かは未確認。

## 修正レーン判断

- 人間が確認した不具合として扱う。
- 現時点では原因未確認のため、恒久修正へ進めない。
- 既存の修正前調査は、空表示全体を前提にした表現を含むため、補足調査が必要である。
- 既存の補足調査は、再起動を手順として固定していないため、初回起動時の再現証跡としては不足である。
