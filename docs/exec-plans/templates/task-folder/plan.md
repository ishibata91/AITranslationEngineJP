# Task Plan: <task-id>

`plan.md` は branch 情報と、この task でやること・やらないことの要点を持つ。
設計は `design.md`、確定仕様は `spec.md`、恒久的に残す判断は `docs/changelog.md` が持つ。

## やること

- <この task で何をするかの要点を書く。人間の依頼内容をそのまま要約する。後から plan.md 単体で何の task か分かる粒度にする。設計判断と手段選定は design.md が持つ。>

## branch 情報

- `execution_branch`: `claude/<task-id>`
- `target_branch`: `master`
- `source_commit`: <分岐元 commit hash>

## やらないこと

- <この task で扱わない範囲の要点を書く。goal に要る手段が対象として残っているかを確かめる。>
