id: P1-I01
title: Add xEdit export JSON importer entrypoint
type: impl
status: done
goal: Accept xEdit export JSON input through a backend entrypoint that can later expand
  to multi-file import.
owned_scope:
- src-tauri/src/application/importer/
- src-tauri/src/gateway/commands.rs
- src-tauri/src/infra/xedit_export_importer.rs
out_of_scope:
- job orchestration
- UI wiring
depends_on: []
produces:
- backend import entrypoint with file-path collection input
consumes:
- xEdit export JSON files
acceptance_anchor:
- valid xEdit export JSON import succeeds through the backend boundary
parallel_safe_with:
- P1-C01
- P1-C02
shared_risks:
- expanding importer scope into job logic would erase future task boundaries
done_when:
- the backend can import xEdit export JSON through a stable entrypoint without UI
  coupling
