---
name: test-design
description: Codex 側のテスト設計作業プロトコル。承認済み設計または修正レーン入力から active plan 内にテスト観点表または E2E テスト観点差分を固定する。
---
# Test Design

## 目的

`test-design` は作業プロトコルである。
`test_designer` agent が承認済み設計を読み、active plan 内にテスト観点表を固定する時の判断基準を提供する。
`test_designer` agent が修正レーン入力を読み、E2E テスト観点正本との差分だけを active plan 内に固定する時の判断基準を提供する。

## 対応ロール

- `test_designer` が使う。
- 呼び出し元は `implement_lane` または `fix_lane` とする。
- 返却先は `implement_lane` または `fix_lane` とする。
- 担当成果物は `テスト設計` または `E2E テスト観点差分` とする。

## 呼び出し元から渡される情報

- 作業計画フォルダ: テスト観点表を書き戻す `docs/exec-plans/active/<task-id>/`。
- 承認済み詳細仕様差分: テスト観点の仕様根拠にする `detail-spec-diff.md`。
- 承認済み画面設計差分: UI 人間操作 E2E の selector と対象画面の根拠にする `screen-design-diff.<screen-id>.md`。
- 関連ユースケース: テスト観点の `関連UC` に対応する docs 正本または task 内成果物。
- 承認記録: 詳細仕様差分、画面設計差分、関連ユースケースをテスト設計根拠として扱ってよい人間承認。
- 人間観測記録: 修正レーンで人間が見た画面、操作、ログ、失敗、期待との差分。
- 修正方針判断: 修正レーンで `fix_decider` が返した原因の原因、責務境界、採用する修正方針、禁止する修正。
- UC 差分候補: 修正レーンで確認した既存ユースケース正本との差分有無、記述不足候補、新規判断必要箇所。
- E2E テスト観点正本: 修正レーンで差分比較の基準にする `docs/e2e-test-design/test-design.csv`。
- 非必須入力: 既存テスト観点表。

## 作業前に読む正本

- エージェント実行定義と実行境界は [test_designer.toml](/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/agents/test_designer.toml) に従う。
- E2E テスト規約は [e2e-test-guidelines.md](/Users/iorishibata/Repositories/AITranslationEngineJP/docs/e2e-test-guidelines.md) とする。
- テストコーディング規約は [coding-guidelines-tests.md](/Users/iorishibata/Repositories/AITranslationEngineJP/docs/coding-guidelines-tests.md) とする。
- 画面設計書正本は [screen-design](/Users/iorishibata/Repositories/AITranslationEngineJP/docs/screen-design/README.md) とする。
- 詳細仕様正本は [detail-specs](/Users/iorishibata/Repositories/AITranslationEngineJP/docs/detail-specs/README.md) とする。
- 新規実装レーン入口は [implement-lane](/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/implement-lane/SKILL.md) とする。
- 修正レーン入口は [fix-lane](/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/fix-lane/SKILL.md) とする。
- E2E テスト観点正本は [test-design.csv](/Users/iorishibata/Repositories/AITranslationEngineJP/docs/e2e-test-design/test-design.csv) とする。
- 外部成果物 が不足または衝突する場合は停止し、衝突箇所を返す。

## skill 内の拘束条件

テスト観点表は active plan 内の `test-design.csv` に置く。
CSV header は次に固定する。

```csv
ID,関連UC,対象画面,前提条件,手順,期待値,備考
```

各列の意味は次の表に従う。

| 列 | 意味 |
| --- | --- |
| `ID` | テスト観点を一意に識別する ID。 |
| `関連UC` | 関連するユースケース。 |
| `対象画面` | 操作または検証の主対象画面。 |
| `前提条件` | テスト開始時に利用者が画面上で確認できる表示状態。 |
| `手順` | selector、操作対象、操作種別、入力値を指定した操作手順。 |
| `期待値` | selector、状態変化、表示内容、後続導線を指定した期待結果。 |
| `備考` | 補足、制約、未決事項。 |

テスト分類は次の表に従う。
この表は、関連ユースケースから拾う観点を拘束する。

| 分類 | 意味 |
| --- | --- |
| 正常 | 利用者が目的を達成する標準経路。 |
| 代替 | 利用者が目的を中断、取り消し、検索結果なし、差分なしなどで終える経路。 |
| 例外 | 入力不正、資格情報不足、形式不正、実行条件不足などで目的を達成できない経路。 |
| 境界 | 無効状態、重複実行、件数 0、状態遷移直前直後など、仕様の端を確認する経路。 |

E2E テスト観点差分は active plan 内の `e2e-test-design-diff.csv` に置く。
CSV header は次に固定する。

```csv
ID,関連UC,対象画面,前提条件,手順,期待値,備考
```

E2E テスト観点差分の分類は次の表に従う。

| 分類 | 意味 |
| --- | --- |
| `差分なし` | E2E テスト観点正本に修正対象を証明する観点が既にある状態。 |
| `追加候補あり` | E2E テスト観点正本に対し、修正対象を証明する観点が不足している状態。 |
| `判断不足` | 人間レビューで期待値または対象画面を確認しないと観点を固定できない状態。 |

## 担当ロールが判断してよい範囲

- 承認済み詳細仕様差分、承認済み画面設計差分、関連ユースケースだけをテスト観点の根拠にする。
- 修正レーンでは、人間観測記録、修正方針判断、UC 差分候補、E2E テスト観点正本だけを E2E テスト観点差分の根拠にする。
- UI 人間操作 E2E は画面操作を既定の開始点にする。
- `data-testid` は画面設計時点で固定する selector として扱う。
- `手順` は selector レベルで書く。
- `期待値` は selector レベルで書く。
- 前提条件は実行順依存ではなく、各テストが単独実行に必要な画面表示状態として書く。
- 前提条件には repository、DB、fake、seed、helper、fixture、保存先、外部境界の準備手順を書かない。
- 前提条件には、表示中の画面全体領域、対象行、対象入力領域、対象操作ボタン、状態表示、エラー表示などを selector または画面文言で書く。
- 前提データ投入は主シナリオの証明ではなく状態準備として扱い、テスト観点表では画面上に現れた状態として表す。
- 1 行は 1 つのテスト観点だけを表す。
- 関連ユースケースごとに、正常、代替、例外、境界の分類から必要な観点を拾う。
- 修正レーンでは、E2E テスト観点正本へ既にある観点を重複して追加候補にしない。
- 修正レーンでは、差分なしの場合でも `差分なし` と根拠を返す。
- 修正レーンでは、期待値または対象画面の判断が不足する場合でも停止せず、`判断不足` と確認事項を返す。
- 修正レーンでは、UC 差分候補が記述不足の場合、既存期待の明文化不足として扱える範囲で E2E テスト観点差分を作る。
- 正常系だけでテスト観点表を閉じない。
- 手順はテスト実装へ移せる粒度で書く。
- 手順には selector の適用範囲、操作対象、操作種別、入力値を含める。
- 手順には `開始を押す`、`手順開始を押す`、`登録操作を押す`、`対象行を押す` のような操作対象が曖昧な表現を書かない。
- 操作不可を確認する観点では、クリック操作を実行せず、対象操作ボタンの無効状態または無効理由を確認する手順として書く。
- AI へリクエストする正常系は、送信中または確認中または生成中の表示、実行中の操作制限、完了件数、結果行、後続導線を期待値に含める。
- 一覧、検索、削除、取り消し、再出力、差分確認の観点では、対象行の維持、消失、該当なし、差分なしなどの画面表示を期待値に含める。
- 未確定 selector は独断で補完せず、`備考` に未決として書く。
- 未確定 selector が複数画面または複数行へ広がる場合は、active plan 内の `data-testid-gaps.md` に資料化する。
- 実装手順、テスト実装、検証コマンドはテスト観点表に含めない。

## skill が扱わない対象

- プロダクトコード、プロダクトテスト、docs 正本本文は変更しない。
- `implementation-scope.md` は作らない。
- シナリオテスト実装と単体テスト実装は扱わない。
- 未承認の詳細仕様差分、画面設計差分、関連ユースケースは根拠にしない。
- 修正レーンでは、E2E テスト観点正本そのものを変更しない。

## 返す成果物

- 判断結果: テスト設計を完了したか、文脈不足で停止したかを返す。
- 根拠参照: テスト観点表または E2E テスト観点差分の根拠にした資料を返す。
- テスト観点表: active plan 内に作成または更新した `test-design.csv` を返す。
- E2E テスト観点差分: active plan 内に作成または更新した `e2e-test-design-diff.csv` を返す。
- E2E テスト観点差分分類: 差分なし、追加候補あり、判断不足のいずれかを返す。
- selector 不足資料: active plan 内に作成または更新した `data-testid-gaps.md` を返す。
- 不足情報: 不足した入力項目、衝突した根拠、戻し先を返す。
- 次判断材料: 返却先が後続の実装範囲、シナリオテスト、単体テストの扱いを判断できる材料を返す。
- 禁止事項: 出力にプロダクトコード、プロダクトテスト、docs 正本本文の変更を含めない。

## 作業を完了できる条件

- 新規実装レーンでは `test-design.csv` が active plan 内に存在する。
- 新規実装レーンでは `test-design.csv` が固定 CSV header を持つ。
- 修正レーンでは `e2e-test-design-diff.csv` が active plan 内に存在する。
- 修正レーンでは `e2e-test-design-diff.csv` が固定 CSV header を持つ。
- 修正レーンでは E2E テスト観点差分分類が差分なし、追加候補あり、判断不足のいずれかで返っている。
- 各行の `関連UC` が関連ユースケースに対応している。
- 各行の `対象画面` が画面設計差分または画面設計書正本に対応している。
- 各行の `手順` と `期待値` が selector、状態変化、表示内容を特定できる粒度で書かれている。
- 各行の `前提条件` が単独実行に必要な画面表示状態として書かれている。
- 各行の `前提条件` に repository、DB、fake、seed、helper、fixture、保存先、外部境界の準備手順が残っていない。
- `手順` に `開始を押す`、`手順開始を押す`、`登録操作を押す`、`対象行を押す` のような曖昧な操作が残っていない。
- 関連ユースケースから必要な正常、代替、例外、境界の観点が抽出されている。
- AI へリクエストする正常系の期待値に、処理中表示、操作制限、完了件数、結果行、後続導線が含まれている。
- 未確定 selector がある場合は `備考` に未決として書かれている。
- 未確定 selector が複数画面または複数行へ広がる場合は、active plan 内に `data-testid-gaps.md` が存在する。
- `data-testid-gaps.md` が存在する場合は、対象画面、対象要素、必要 selector、関連テスト ID が書かれている。
- プロダクトコード、プロダクトテスト、docs 正本本文を変更していない。

## 作業を止める条件

- 作業計画フォルダが不足する場合は停止する。
- 新規実装レーンで承認済み詳細仕様差分が不足する場合は停止する。
- 新規実装レーンで関連ユースケースが不足する場合は停止する。
- 新規実装レーンで UI 人間操作 E2E が必要なのに画面設計差分または画面設計書正本が不足する場合は停止する。
- 新規実装レーンで承認記録が不足する場合は停止する。
- 根拠間に衝突がある場合は停止する。
- 新規実装レーンで関連ユースケースから必要な正常、代替、例外、境界を判断できない場合は停止する。
- 新規実装レーンで UI 人間操作 E2E に必要な画面表示状態を根拠から判断できない場合は停止する。
- 新規実装レーンで AI へリクエストする観点の処理中表示、完了表示、結果表示、後続導線を根拠から判断できない場合は停止する。
- 修正レーンで人間観測記録が不足する場合は停止する。
- 修正レーンで修正方針判断が不足する場合は停止する。
- 修正レーンで UC 差分候補が不足する場合は停止する。
- 修正レーンで E2E テスト観点正本が不足する場合は停止する。
- プロダクトコード、プロダクトテスト、docs 正本本文の変更が必要な場合は停止する。
- 停止時は不足項目、衝突箇所、戻し先を返す。
