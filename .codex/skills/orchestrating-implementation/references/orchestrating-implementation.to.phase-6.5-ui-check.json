{
  "contract_version": "2026-04-12",
  "direction": "directing-to-skill",
  "from_skill": "orchestrating-implementation",
  "to_skill": "phase-6.5-ui-check",
  "purpose": "実装済み UI を `chrome-devtools` で確認し、主要導線、画面状態、承認済み HTML モック artifact に対する視覚構造差分の証跡を返させる。",
  "required": [
    {
      "description": "承認済み active exec-plan。",
      "field": "active_exec_plan",
      "type": "path"
    },
    {
      "description": "承認済み UI モック artifact path。",
      "field": "ui_artifact_path",
      "type": "string"
    },
    {
      "description": "承認済み Scenario テスト一覧 artifact path。",
      "field": "scenario_artifact_path",
      "type": "string"
    },
    {
      "description": "確認対象の承認済み task_id 一覧。active exec-plan の `実装計画.tasks[].task_id` に一致していること。",
      "field": "implementation_task_ids",
      "type": "string[]"
    },
    {
      "description": "UI確認前に読む承認済み required reading 一覧。",
      "field": "implementation_required_reading",
      "type": "string[]"
    },
    {
      "description": "設計差分確認に使う review 用差分図 path。差分図がない task では空配列を渡す。",
      "field": "review_diff_diagrams",
      "type": "string[]"
    },
    {
      "description": "実装済み scope と touched files。",
      "field": "implemented_scope",
      "type": "string[]"
    },
    {
      "description": "第6段階の validation 結果。",
      "field": "validation_results",
      "type": "string[]"
    }
  ],
  "optional": [
    {
      "description": "起動 URL、起動手順、確認対象導線。既定では `wails dev` 後の `http://localhost:34115` を渡す。",
      "field": "ui_entry_points",
      "type": "string[]"
    },
    {
      "description": "UI確認で優先すべき論点。視覚構造差分の確認対象を含めてよい。",
      "field": "focus_points",
      "type": "string[]"
    }
  ],
  "must_not_change": [
    "新しい仕様解釈を追加しない",
    "承認済み artifact と差分図にない設計差分を推測で補わない"
  ],
  "completion_signal": [
    "orchestrator が承認済み design bundle 基準で unit test phase か implementation phase への戻し先を決められる",
    "orchestrator が承認済み HTML モック artifact に対する視覚構造差分の有無を基準に第6段階への reroute を決められる"
  ]
}
