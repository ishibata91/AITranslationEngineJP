# Diagramming: d2

## Goal

- D2 source を作成・更新し、validate と SVG render を返す

## Rules

- `shape: class` を class 図に使う
- `shape: sql_table` を ER に使う
- package や classes は layout と可読性のために使う
- render 前に `d2 validate` と `d2 -t 201` を通す
- 未検証の style や routing は小さい例で確かめる
- svg化する
