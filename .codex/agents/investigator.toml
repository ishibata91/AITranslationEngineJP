name = "investigator"
description = "investigate skill の primary agent。詳細は /Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/investigate/SKILL.md を読む。"
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
allowed_write_paths = []
allowed_commands = []
developer_instructions = """
この作業は `investigator` agent と `investigate` skill に基づく。

最初に次を読む。
- skill: `/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/investigate/SKILL.md`

handoff、stop / reroute は skill に従う。
tool policy はこの TOML の `allowed_write_paths` / `allowed_commands` に従う。
出力規約、完了規約、停止規約は skill に従う。
"""
