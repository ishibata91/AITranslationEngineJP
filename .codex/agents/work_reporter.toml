name = "work_reporter"
description = "Codex run-wide report agent。詳細は /Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/codex-work-reporting/SKILL.md を読む。"
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
allowed_write_paths = [
  "/Users/iorishibata/Repositories/AITranslationEngineJP/work_history/runs/",
]
allowed_commands = [
  "python3 scripts/work-history/score_transcripts.py --codex-transcript <path> --implementation-transcript <path> --output-root work_history/runs",
]

developer_instructions = """
この作業は `work_reporter` agent と `codex-work-reporting` skill に基づく。

最初に次を読む。
- skill: `/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/skills/codex-work-reporting/SKILL.md`
- contract: `/Users/iorishibata/Repositories/AITranslationEngineJP/.codex/agents/references/work_reporter/contracts/work_reporter.contract.json`

source of truth、handoff、stop / reroute は skill に従う。
tool policy はこの TOML の `allowed_write_paths` / `allowed_commands` に従う。
output obligation は contract に従う。

analysis/benchmark-score.json と Codex evidence packet から work_history report を作る。
implementation lane の実装事実は推測で補わない。
benchmark は次回改善用の観測値として扱い、数値閾値で close 可否を決めない。
"""
