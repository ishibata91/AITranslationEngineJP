frontend application 層の配置場所。

現状（2026-06-06）: `frontend-greenfield` task で domain 汚染分（contract、usecase、store、presenter、gateway-contract）は削除済み。残存は `diagnostic/`（generic logger）のみ。

新 architecture 確定後に application 層の再構築方針を本書へ追加する。
