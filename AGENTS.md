# AGENTS.md

## index

| パス | 種別 | 概要 |
| --- | --- | --- |
| `assets/` | フォルダ | アプリケーションに同梱する静的資産を置く。 |
| `cmd/` | フォルダ | 開発用または検証用の Go コマンドを置く。 |
| `db/` | フォルダ | SQLite schema と migration 適用を置く。 |
| `dictionary/` | フォルダ | 翻訳辞書の管理機能と MCP 公開機能を置く。 |
| `docs/` | フォルダ | 設計、仕様、作業計画を置く。人間用。基本的にプロンプトに載せない。 |
| `frontend/` | フォルダ | Svelte によるデスクトップアプリケーションの表示層を置く。 |
| `internal/` | フォルダ | Go backend の翻訳処理、永続化、外部 AI 接続を置く。 |
| `scripts/` | フォルダ | 開発、検証、品質確認を自動化するスクリプトを置く。 |
| `tools/` | フォルダ | 本体プロセスから分離した補助ツールを置く。 |
| `main.go` | ファイル | Wails アプリケーションの起動点を置く。 |
| `appdir.go` | ファイル | アプリケーション用ディレクトリの解決処理を置く。 |
| `logging_dev.go` | ファイル | 開発時のログ出力設定を置く。 |
| `logging_production.go` | ファイル | 本番時のログ出力設定を置く。 |

## 責務

- ルートは Wails アプリケーションの起動、プロダクト全体の設定、各実装階層の配置を担当する。
- 各実装階層の詳細な責務は、最も近い `AGENTS.md` に置く。

## 実装規約

- Wails アプリケーションの起動点は `main.go` に置く。
- 実装コードは責務に対応する下位フォルダに置く。

## プロトコル
- 日本語で返答する
- 口調は常体
- 1文1論点
- 指示語禁止
- 一般語，ユビキタス以外は説明的に
- 事実と推測は分ける。
- 人間向けに順序・依存関係を説明するときはmermaidコードブロックにして説明する
- 広範な探索は基本的に探索サブエージェントに移譲する。

## 用語集
メインエージェント=人間と接している主体
サブエージェント=メインエージェントから分離された作業者
fresh=コンテキストをメインエージェントから引き繋がないサブエージェント
fork=コンテキストをメインエージェントから引き継ぐサブエージェント


## コマンド
devサーバー起動　`npm run dev:wails:run`
接続先　`http://localhost:34115`
