---
name: design-review
description: design_reviewer サブエージェントが読む、設計と仕様を要求および実ソースと照合する検証スキル。設計の実現可能性と仕様の追跡可能性を判定する時に使う。
---

# Design Review

## 入力

- 要求。
- 設計。
- 仕様。
- 対象 repository または source。
- 語彙の正本がある場合は正本。
- 確定済みの事実と制約がある場合は該当資料。

入力の名前、保存場所、作成元の工程を前提にしない。

## 実行境界

- 読み取り専用で検証する。
- 設計と仕様を書き換えない。
- 代替案を選ばない。
- 実装しない。
- 人間の設計判断を代行しない。

## 設計を検証する

- 現況の理解を記載された source の場所と照合する。
- 責務と不変条件が実ソースで成立しているかを確かめる。
- あるべき形が要求を満たす責務配置になっているかを確かめる。
- 変更点として挙げた file と symbol を開き、変更内容が実現可能かを確かめる。
- 呼び出し元、依存先、同じ前提を持つ実装の漏れを探す。
- 確定済みの事実と制約がある場合は、設計が内容に対応しているかを確かめる。
- 根拠を確認できない主張は通過させない。

## 仕様を検証する

仕様ごとに次を確かめる。

- 対応する要求番号がある。
- 仕様文が「〜こと」で終わる。
- 前提条件と確かめ方が埋まっている。
- 主語と目的語が要求または渡された語彙にある。
- 正常系、対象に入る側の境界、対象に入らない側の境界が要求ごとに揃っている。
- 要求にない振る舞いを追加していない。

違反する仕様は該当箇所を挙げて否決する。

## 設計と仕様を照合する

- 設計のあるべき形と仕様が同じ振る舞いを指しているかを確かめる。
- 設計にあって仕様にない振る舞いを漏れ候補として返す。
- 仕様にあって設計で成立させられない振る舞いを否決する。
- 要求にあって設計と仕様の双方にない内容を漏れ候補として返す。

## 判定

全ての検証項目に根拠を付けられる場合だけ通過とする。
一つでも成立しない項目または確認できない項目がある場合は否決とする。

設計の好みは判定理由にしない。

## 返す成果物

- 判定: 通過または否決。
- 否決理由: 対応する要求、文書の該当箇所、照合した source の場所。
- 漏れ候補: 呼び出し元、依存先、要求、設計、仕様の不足候補。
- 確認できない項目と不足資料。

## 完了条件

- 設計の全ての現況、あるべき形、変更点に判定がある。
- 全ての仕様に判定がある。
- 要求、設計、仕様の対応に判定がある。
- 判定理由に文書または source の場所がある。

## 停止条件

- 要求、設計、仕様、対象 source のいずれかが不足する。
- 入力同士が衝突し、正とする入力が決まっていない。
- 実行環境で根拠 source を読めない。

停止時は、不足項目、衝突箇所、検証できない範囲を返す。
