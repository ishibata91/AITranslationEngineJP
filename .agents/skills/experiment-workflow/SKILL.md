---
name: experiment-workflow
description: "反復実験フローの再導入後に使う予定の技能。現在は使用しない。固定アイデアの比較評価には idea-evaluation を使う。"
---

# Experiment Workflow

反復構造は停止中とする。

変更案を繰り返し改善する task は開始しない。

固定したアイデアを砂場で一度試す task は `idea-evaluation` を使う。

既存の解決法、論文、公開リソースを探す task は `research-protocol` を使う。
